# NeuroSLAM — Python 依赖 (一键安装: pip install -r requirements.txt)
# 如需 CARLA 数据采集，还需要安装 carla wheel:
#   py -3.12 -m pip install <CARLA_DIR>\PythonAPI\carla\dist\carla-*.whl

numpy>=1.19.0
scipy>=1.5.0
opencv-python>=4.5.0
pandas>=1.1.0
matplotlib>=3.3.0
